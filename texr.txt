Mon 22 Jun — Auditor precision rules (exemptions & risk signals)
Added Pfizer/major-vendor action exemptions to stop flagging trusted internal actions. Implemented the cron/scheduled rule (unattended workflows held to a stricter standard) and a "references secrets" risk signal that escalates findings in workflows that touch secrets.

Tue 23 Jun — AWS & App-RSA secret classification
Used the Tableau secrets metadata to calibrate new checks that classify AWS credentials and GitHub App RSA private keys. Built the classifier, tested it, and opened PR #5 with the auditor changes.

Wed 24 Jun — Reporting refresh
Regenerated the Excel findings + inventory reports against the latest 23-org scan (~88k findings). Added new "AWS & App-RSA" and "Scheduled risk" breakdown sheets and summary KPIs.

Thu 25 Jun — Power BI dashboards
Migrated reporting to Power BI for leadership visibility. Built the import workbook (findings + inventory tabs with status/snapshot tracking) and set up the report in the browser — first severity charts done.

Fri 26 Jun — Review feedback / false-positive tuning
Worked through Philippe's review comments. Narrowed the AWS check to real credentials only (ignores region/account/S3) and the inline-install check to ignore safe cases (pip install --upgrade pip, version-pinned, lockfile installs) — ~3k fewer noise findings. Triaging the one under-rated case (unpinned install in a deploy job) for severity. Posted standup.
